# Football-Match-Result-Prediction-System-Using-Random-Forest
FINAL YEAR PROJECT

NOTE: This project uses a JSON file generated from the model loaded via JavaScript. Browsers block fetching local files for security, so you need to run a local server (Live Server, Python, XAMPP) to make it work Properly.
